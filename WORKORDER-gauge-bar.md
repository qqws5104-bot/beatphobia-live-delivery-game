# 작업지시서 — 엘리베이터 게이지 바

> **선행 조건**: `HANDOVER.md`를 먼저 끝까지 읽을 것. 특히 6절(절대 건드리면 안 되는 것)과
> 7절(현재 엘리베이터 화면 구조).

---

## 0. 한 줄 요약

엘리베이터 위치 표시를 **"층 목록 + 한 줄 하이라이트"** 에서
**"눈금 6칸짜리 세로 게이지 바"** 로 교체한다. **화면 표현만 바꾼다. 게임 로직은 손대지 않는다.**

---

## 1. 배경 — 왜 하는가

엘리베이터는 **두 사람이 하나를 공유하는 줄다리기**다. 5초 동안 서로 ▲/▼를 연타하고,
누가 누르든 그 즉시 실제 층이 한 칸씩 움직이며, 5초가 끝나는 순간 서 있는 층이 배송 위치가 된다.
이 메커니즘은 **이미 구현되어 정상 동작 중이다** (commit `527891b`).

문제는 화면이다. 지금은 층 6개가 목록으로 쌓여 있고 현재 층 한 줄에만 색이 들어온다.
연타로 층이 요동쳐도 **하이라이트가 줄 사이를 조용히 옮겨다닐 뿐**이라,
"밀고 당기는 중"이라는 느낌이 전혀 안 난다. 게임의 핵심 긴장감이 시각적으로 죽어 있다.

→ 이걸 **차오르고 빠지는 게이지 바**로 바꿔서, 연타의 팽팽함이 눈에 보이게 한다.

---

## 2. 범위

### 2.1 바꿀 것 (이것만)

`build_client.py` 단 한 파일:

| 대상 | 위치 |
|---|---|
| 샤프트 CSS | `HEAD_HTML` 안, `.shaft` / `.shaft-track` / `.floor-stop` / `.car` 규칙 (대략 262~268행) |
| 샤프트 렌더 함수 | `APP_JS_TEMPLATE` 안 `renderShaft(floorIdx)` |

부수적으로 필요하면:
- `test_nudge.js` — 셀렉터만 새 마크업에 맞게 수정 (2.3 참조)
- `screenshot_nudge.js` — `.shaft` 셀렉터를 새 루트 요소명으로 (이름을 바꿀 경우)

### 2.2 건드리지 말 것

- **`game-room.js`** — 이동/배송/점수/라운드 로직 전부. 이미 동작하며 테스트로 고정되어 있다.
- **`server.js`**, **`game-data.js`** — 변경 사유 없음.
- **`renderElevator`의 오른쪽 패널** — 버튼, 클릭 카운터, 라운드 결과, 준비 칩, 송장 목록. 그대로.
- **정보 공개 규칙** — 상대 송장/클릭 수는 계속 숨김 (`HANDOVER.md` 3.4절).
- **`public/index.html` 직접 편집** — 빌드 산출물이다.

> **재구현 금지.** "1클릭 = 1칸", "초 단위 라운드", "상대 이동 실시간 표시"는
> 이미 서버에 구현되어 있다. 클라이언트에서 다시 만들거나 흉내내지 말 것.

### 2.3 테스트가 깨질 지점 (미리 인지)

`test_nudge.js`가 `.floor-stop.current` 셀렉터로 현재 층을 읽는다:

```js
document.querySelector(".floor-stop.current").textContent.trim()   // → "3F"
document.querySelectorAll(".floor-stop.current").length            // → 정확히 1
```

마크업을 바꾸면 이 테스트가 깨진다. 둘 중 하나로 처리:

- **(권장)** 새 마크업에서도 각 눈금에 `.floor-stop`, 현재 칸에 `.current`를 그대로 유지하고,
  눈금 안에 층 이름 텍스트를 남긴다 → 테스트 무수정 통과.
- 또는 새 셀렉터로 `test_nudge.js`를 수정한다. **단, 검증 의미(어느 층인지 / 정확히 하나인지)는
  절대 약화시키지 말 것.** 테스트를 통과시키려고 단언을 무르게 만드는 것은 금지.

`test_hosted.js`는 샤프트 마크업에 의존하지 않는다 — 수정 불필요.

---

## 3. 요구사항

### 3.1 필수 (확정된 사양)

| # | 요구사항 |
|---|---|
| R1 | **세로 방향.** 현재와 같은 위치(`.elev-layout` 왼쪽 220px 열). 위가 5F, 아래가 B1. |
| R2 | **눈금 6칸 유지.** B1 / 1F / 2F / 3F / 4F / 5F 경계가 명확하게 보일 것. 연속 게이지 아님. |
| R3 | **1클릭 = 눈금 1칸.** 항상 6칸 중 한 칸에 정확히 걸려 있고, 중간에 애매하게 걸치지 않는다. |
| R4 | **어느 층인지 항상 명확히 읽힐 것.** 층 이름이 화면에 남아 있어야 한다. 배송 주소가 `401호`처럼 층에 묶여 있어서, "지금 몇 층인지"가 모호하면 게임이 성립하지 않는다. |
| R5 | **양쪽 화면에서 동일하게 보일 것.** 상대가 누른 이동도 내 화면에서 똑같이 움직여야 한다. |
| R6 | **차오름/빠짐이 시각적으로 드러날 것.** 단순히 색이 켜졌다 꺼지는 게 아니라, 게이지가 **자라나고 줄어드는** 것으로 읽혀야 한다. |
| R7 | 층 변화에 **짧은 전환 애니메이션**. 연타 시 게이지가 출렁이는 느낌이 나야 한다. 단, **다음 클릭을 지연시키면 안 된다** — 애니메이션 중에 새 상태가 오면 즉시 그쪽으로 따라가야 한다. (권장: 150~250ms, `ease-out`) |

### 3.2 설계 재량 (알아서 판단할 것)

다음은 지정하지 않는다. 위 요구사항을 만족하는 선에서 판단할 것:

- 게이지의 구체적 형태 (통짜 막대 + 눈금선 / 6개 세그먼트 적층 / 그 외)
- 채워진 부분과 빈 부분의 색·질감 처리
- 층 이름을 게이지 안에 넣을지, 옆에 눈금 라벨로 뺄지
- 현재 층을 추가로 강조할지 (숫자 뱃지, 테두리 등)

**기존 디자인 토큰을 쓸 것.** 새 색을 발명하지 말 것:
`--gold: #f0b84a` (강조/현재), `--sky: #6dbbfd`, `--bg-deep: #0a1220` (바탕),
`--panel-line: rgba(255,255,255,0.09)` (경계), `--muted: #93a0be` (비활성),
`--font-display: 'Oswald','Noto Sans KR'` (층 이름 같은 표시용 텍스트).

### 3.3 주의사항

- **`.shaft-track`은 현재 `flex-direction: column-reverse`다.** DOM 순서(B1→5F)와 화면 순서(5F→B1)가
  반대. 이 방식을 유지하든 바꾸든, **`querySelectorAll(...)[i]`가 `FLOORS[i]`에 대응**한다는 전제는
  테스트가 의존하므로 유지하거나, 바꿀 거면 테스트도 함께 정확히 맞출 것.
- 세로 공간이 넉넉하지 않다. 노트북 한 화면에 들어가야 한다는 요구로 이미 한 번 레이아웃을 조인 이력이 있다.
  게이지 바가 세로로 과하게 길어지지 않게 할 것.
- `renderShaft`는 **매 상태 브로드캐스트마다 호출된다** (클릭 하나당 한 번). DOM을 통째로 다시 그리는 구조라
  CSS transition이 리셋될 수 있다 — R7을 만족시키려면 이 점을 고려해서 구현할 것.
  (전체 재생성 대신 게이지 채움 요소만 갱신하거나, transition이 유지되는 방식으로 처리)
- 반응형: `@media (max-width:820px)`에서 `.elev-layout`이 1열로 접힌다. 이때도 깨지지 않아야 한다.

---

## 4. 작업 절차

```bash
# 1. 수정
#    build_client.py 의 CSS + renderShaft

# 2. 빌드
python3 build_client.py

# 3. 테스트용 타이머 임시 단축 (HANDOVER.md 5.3절 그대로)
#    SECURE_PHASE_MS: 3 * 60 * 1000 → 6 * 1000
python3 build_client.py
node server.js &

# 4. 검증
node test_nudge.js        # 2회 연속 통과할 것
node test_hosted.js       # 2회 연속 통과할 것
node screenshot_nudge.js  # 스크린샷 육안 확인

# 5. 타이머 원복 (필수!)
#    SECURE_PHASE_MS: 6 * 1000 → 3 * 60 * 1000
python3 build_client.py
grep SECURE_PHASE_MS game-data.js   # '3 * 60 * 1000' 확인

# 6. 커밋 + 배포
git add build_client.py public/index.html [수정한 테스트 파일]
git commit -m "..."
git push origin main      # → Render 자동 배포
```

> `public/index.html`을 커밋에 반드시 포함할 것. 빼먹으면 소스만 바뀌고 배포 화면은 그대로다.

---

## 5. 완료 기준

기능:
- [ ] ▲/▼ 클릭 또는 ↑/↓ 키 입력마다 게이지가 **한 칸씩 즉시** 오르내린다.
- [ ] **상대가 누른 이동도** 내 화면에서 동일하게 보인다. (2개 브라우저 컨텍스트로 확인)
- [ ] 게이지는 항상 6칸 중 **정확히 한 칸**에 걸려 있고, 현재 층 이름이 명확히 읽힌다.
- [ ] B1과 5F에서 더 눌러도 범위를 벗어나지 않는다.
- [ ] 5초 종료 시점의 층 = 배송 처리된 층 (라운드 결과 화면과 일치).

회귀 없음:
- [ ] `node test_hosted.js` 2회 연속 통과.
- [ ] `node test_nudge.js` 2회 연속 통과.
- [ ] 상대 송장 목록 / 상대 클릭 수가 여전히 노출되지 않는다.
- [ ] 종료 화면은 여전히 양쪽 전체 명세를 보여준다.
- [ ] `game-data.js`의 `SECURE_PHASE_MS`가 `3 * 60 * 1000`으로 원복되어 있다.
- [ ] `game-room.js` / `server.js` / `game-data.js`에 **변경 없음** (`git diff`로 확인).

검수 자료:
- [ ] 게이지가 서로 다른 층에 있는 상태 스크린샷 최소 2장.
- [ ] 상대 클릭이 반영된 시점의 **양쪽 화면** 스크린샷 (동일 층인지 대조 가능하게).

---

## 6. 판단이 필요하면

다음은 **임의로 결정하지 말고 물어볼 것**:

- 요구사항끼리 충돌해서 어느 하나를 포기해야 할 때.
- 세로 공간이 부족해 게이지를 줄이거나 오른쪽 패널을 건드려야 할 때.
- R7(애니메이션)을 만족시키려면 서버/브로드캐스트 구조를 손대야 한다고 판단될 때
  → **이 경우 특히 반드시 물어볼 것.** 서버 단일 진실 원칙을 깨는 방향이면 십중팔구 잘못된 접근이다.
